//list.c

typedef struct shelf {
  char row;   // A-Z
  int column; // 0-99
} shelf_t;

typedef struct link {
  struct shelf shelf;
  struct link *next;
} link_t; 

typedef struct list {
  struct link *head;
  struct link *tail;
} list_t;
